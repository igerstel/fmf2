<p id="notice"><%= notice %></p>

<p>
  <b>Name:</b>
  <%= @produce.name %>
</p>

<p>
  <b>Amount:</b>
  <%= @produce.amount %>
</p>

<p>
  <b>Cost:</b>
  <%= number_to_currency(@produce.cost) %>
</p>

<p>
  <b>Bundle:</b>
  <%= @produce.bundle %>
</p>

<p>
  <b>Category:</b>
  <%= @produce.category %>
</p>

<p>
  <b>Farm:</b>
  <%= @produce.farm.address %>
</p>


<%= link_to 'Edit', edit_produce_path(@produce) %> |
<%= link_to 'Back', produces_path %>






<%= form_for(@produce) do |f| %>
  <% if @produce.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@produce.errors.count, "error") %> prohibited this order from being saved:</h2>

      <ul>
      <% @produce.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :cost %><br />
    <%= f.text_field :cost %>
  </div>
  <div class="field">
    <%= f.label :amount %><br />
    <%= f.text_field :amount %>
  </div>
  
  
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>